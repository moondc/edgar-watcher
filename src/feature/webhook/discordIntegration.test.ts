// import { of } from "rxjs";
// import axiosWrapper from "../../core/api/axiosWrapper";
// import sendMessage from "./discordIntegration";

// jest.mock("../../core/api/axiosWrapper", () => ({
//     __esModule: true,
//     default: {
//         post: jest.fn(),
//     }
// }));
// const axiosWrapperMock = (axiosWrapper as jest.MockedFunction<any>)

// jest.mock("./discordMessageFormatter", () => ({
//     generateMessage: jest.fn(() => "a"),
// }));

describe("sendMessage tests", () => {
    it("delete me",() =>{});
//     afterEach(() => {
//         jest.clearAllMocks();
//     });

//     it("should send a message with formatted content", (done) => {
//         const endpoint = "asdf";
//         const users = ["a", "b"];
//         const message = "c";
//         const urls = ["d", "e"];
//         axiosWrapperMock.post.mockReturnValue(of(7))

//         sendMessage(endpoint, users, message, urls).subscribe((next: any) => {
//             expect(axiosWrapper.post).toHaveBeenCalledWith(endpoint, { content: "a" });
//             done();
//         });
//     });
});